{"task_id": "task_invocation_parameters_hermetic_check", "tier": 1, "type": "rule_only", "distilled_think": "This enforces hermetic builds at the task level, distinct from the pipeline-level hermetic parameter. Each task in the build pipeline should have its own HERMETIC flag set to \"true\".\n\nThe path is `input.predicate.buildConfig.tasks[*].invocation.parameters.HERMETIC` - note the uppercase HERMETIC (Tekton convention for task parameters).\n\nIterating pattern:\n```\nsome task in input.predicate.buildConfig.tasks\ntask.invocation.parameters.HERMETIC != \"true\"\n```\n\nThis fires if ANY task has HERMETIC that isn't the string \"true\". This is important for supply chain security - each individual task needs to declare it's running in a hermetic environment, not just the pipeline overall.\n\nThe sprintf message shows the actual value, which helps debug which tasks are non-hermetic."}
{"task_id": "task_invocation_parameters_image_check", "tier": 1, "type": "rule_only", "distilled_think": "This validates that each task records which container IMAGE it used. The field is `input.predicate.buildConfig.tasks[*].invocation.parameters.IMAGE`.\n\nThe check is:\n```\nsome task in input.predicate.buildConfig.tasks\nnot task.invocation.parameters.IMAGE\n```\n\nThis fires if any task is missing the IMAGE parameter. Knowing which images tasks ran in is critical for:\n1. Reproducibility - can we recreate this build?\n2. Security scanning - are the task images from trusted registries?\n3. Audit trails - which software versions were used?\n\nThe message \"IMAGE is missing\" is generic. In a multi-task pipeline, including task.name would help identify which task is problematic."}
{"task_id": "task_invocation_parameters_tlsverify_check", "tier": 1, "type": "rule_only", "distilled_think": "This enforces TLS verification for task operations. The field is `input.predicate.buildConfig.tasks[*].invocation.parameters.TLSVERIFY`.\n\nThe pattern:\n```\nsome task in input.predicate.buildConfig.tasks\ntask.invocation.parameters.TLSVERIFY != \"true\"\n```\n\nTLSVERIFY ensures that when tasks pull images or artifacts, they verify TLS certificates. This prevents MITM attacks where an attacker could inject malicious dependencies.\n\nSetting TLSVERIFY to false is a common shortcut for development/testing (to work with self-signed certs), but production builds must verify TLS. This rule enforces that production standard.\n\nThe check fires if any task has TLSVERIFY that isn't the string \"true\"."}
{"task_id": "task_name_check", "tier": 1, "type": "rule_only", "distilled_think": "This validates task naming. The field is `input.predicate.buildConfig.tasks[*].name`.\n\nThe pattern:\n```\nsome task in input.predicate.buildConfig.tasks\ntask.name != \"init\"\n```\n\nWait - this seems overly restrictive. This fires if ANY task doesn't have the name \"init\". In a multi-task pipeline, you'd typically have tasks named \"fetch-source\", \"build\", \"push-image\", etc. Requiring ALL tasks to be named \"init\" would mean the pipeline can only have one task.\n\nI suspect the intended use is either:\n1. This is for a single-task pipeline that must be named \"init\"\n2. Or the requirement should be \"at least one task named 'init' must exist\" (different logic)\n\nGiven the code as written, this enforces that every task is named \"init\", which is very specific to a particular pipeline structure."}
{"task_id": "task_ref_params_name_check", "tier": 1, "type": "rule_only", "distilled_think": "Now we're doing nested iteration! This checks task reference parameters. The path is `input.predicate.buildConfig.tasks[*].ref.params[*].name`.\n\nThe Rego pattern uses two `some` statements:\n```\nsome task in input.predicate.buildConfig.tasks\nsome param in task.ref.params\nparam.name != \"name\"\n```\n\nThis is a double loop: for each task, for each param in that task's ref.params array, check if param.name != \"name\". If any param in any task has name != \"name\", deny fires.\n\nSimilar to the previous rule, this seems very restrictive - it requires every parameter of every task reference to be named \"name\", which is oddly self-referential. This might be validating a very specific pipeline structure.\n\nThe nested iteration pattern is the key learning here: when you have arrays within arrays (tasks[*].ref.params[*]), you need nested `some` bindings."}
{"task_id": "task_ref_params_value_check", "tier": 1, "type": "rule_only", "distilled_think": "This validates task reference parameter values using the same nested iteration pattern as the previous rule.\n\nPattern:\n```\nsome task in input.predicate.buildConfig.tasks\nsome param in task.ref.params\nparam.value != \"init\"\n```\n\nDouble loop: for each task → for each param → check if value != \"init\". This fires if any parameter value isn't \"init\".\n\nAgain, this is highly specific - requiring all task reference parameters to have value \"init\". This makes sense if the pipeline has a single standardized parameter setup, but it's very rigid.\n\nThe nested `some` pattern here is idiomatic Rego for checking elements in nested arrays."}
{"task_id": "task_ref_resolver_check", "tier": 1, "type": "rule_only", "distilled_think": "This validates the Tekton task resolver type. The field is `input.predicate.buildConfig.tasks[*].ref.resolver`.\n\nPattern:\n```\nsome task in input.predicate.buildConfig.tasks\ntask.ref.resolver != \"bundles\"\n```\n\nTekton resolvers determine how task definitions are fetched:\n- \"bundles\" = tasks are fetched from OCI bundles (immutable, verifiable)\n- \"git\" = tasks are fetched from git repos (mutable, harder to verify)\n- \"cluster\" = tasks are fetched from in-cluster CRDs\n- \"hub\" = tasks from Tekton Hub\n\nEnforcing \"bundles\" is a good security practice - OCI bundles give you content-addressable, immutable task definitions with cryptographic verification. This prevents supply chain attacks where task definitions are silently modified.\n\nThe check fires if any task uses a resolver other than \"bundles\"."}
{"task_id": "task_result_name_check", "tier": 1, "type": "rule_only", "distilled_think": "This validates task result names using nested iteration. The path is `input.predicate.buildConfig.tasks[*].results[*].name`.\n\nNested pattern:\n```\nsome task in input.predicate.buildConfig.tasks\nsome result in task.results\nresult.name != \"build\"\n```\n\nTasks produce results (outputs like artifact URLs, status codes, etc.). This rule requires every result of every task to be named \"build\", which is again very specific.\n\nIn practice, tasks produce multiple results with different names like \"IMAGE_URL\", \"IMAGE_DIGEST\", \"SUCCESS\". Requiring all results to be named \"build\" suggests this is for a very specialized single-result-per-task pipeline.\n\nThe nested iteration is the key pattern: tasks[*] then results[*] requires two `some` bindings."}
{"task_id": "task_result_type_check", "tier": 1, "type": "rule_only", "distilled_think": "This validates task result types. The path is `input.predicate.buildConfig.tasks[*].results[*].type`.\n\nNested pattern:\n```\nsome task in input.predicate.buildConfig.tasks\nsome result in task.results\nresult.type != \"string\"\n```\n\nTekton results can have types: \"string\", \"array\", \"object\". This rule requires all results to be type \"string\", which is the most common but not the only valid type.\n\nEnforcing string types might be for:\n1. Simplicity - easier to serialize/log\n2. Compatibility - older Tekton versions only supported strings\n3. Security - preventing complex object injection\n\nThe check fires if any result of any task has a non-string type."}
{"task_id": "task_result_value_check", "tier": 1, "type": "rule_only", "distilled_think": "This validates task result values. The path is `input.predicate.buildConfig.tasks[*].results[*].value`.\n\nNested pattern:\n```\nsome task in input.predicate.buildConfig.tasks\nsome result in task.results\nresult.value != \"true\"\n```\n\nThis requires every result value to be the string \"true\", which is extraordinarily specific. If the previous rule requires results named \"build\" and typed \"string\", and this requires value \"true\", we're validating a very rigid structure.\n\nThis might be checking success/status results where \"true\" indicates success. But requiring ALL results to be \"true\" means this pipeline can't output actual data (URLs, digests, etc.) - only success flags.\n\nThe nested iteration pattern remains consistent: task → result → check field."}
{"task_id": "task_results_check", "tier": 1, "type": "rule_only", "distilled_think": "This validates that each task has a results array. The field is `input.predicate.buildConfig.tasks[*].results`.\n\nPattern:\n```\nsome task in input.predicate.buildConfig.tasks\nnot task.results\n```\n\nThis fires if any task is missing the results field. Note: this checks for MISSING but not EMPTY (similar to the materials check earlier). In Rego:\n- task.results undefined → `not task.results` is true → deny ✓\n- task.results = [] → arrays are truthy → `not []` is false → no deny\n\nTo catch both missing AND empty, you'd add: `... or count(task.results) == 0`.\n\nTasks producing no results might be valid (cleanup tasks, notification tasks), but for audit purposes, you might want every task to record at least one result (even if it's just \"status: success\")."}
{"task_id": "task_service_account_name_check", "tier": 1, "type": "rule_only", "distilled_think": "This validates the Kubernetes service account used by tasks. The field is `input.predicate.buildConfig.tasks[*].serviceAccountName`.\n\nPattern:\n```\nsome task in input.predicate.buildConfig.tasks\ntask.serviceAccountName != \"appstudio-pipeline\"\n```\n\nThis enforces that all tasks run under the \"appstudio-pipeline\" service account. Service accounts control:\n1. RBAC permissions - what APIs/resources the task can access\n2. Image pull secrets - which registries the task can pull from\n3. Audit identity - who ran this task\n\nEnforcing a specific service account is good for security - it prevents tasks from escalating privileges by using different, more permissive service accounts. This is a trust boundary.\n\nThe check fires if any task uses a different service account."}
{"task_id": "task_started_on_check", "tier": 1, "type": "rule_only", "distilled_think": "This validates task start timestamps. The field is `input.predicate.buildConfig.tasks[*].startedOn`.\n\nPattern:\n```\nsome task in input.predicate.buildConfig.tasks\nnot task.startedOn\n```\n\nThis fires if any task is missing startedOn. The instruction mentions \"is not a valid ISO-8601 timestamp\" but the implementation only checks presence.\n\nTask timestamps are critical for:\n1. Audit trails - when did each task execute?\n2. Duration calculation - with finishedOn, compute task runtime\n3. Concurrency analysis - which tasks overlapped?\n4. Debugging - correlate task execution with external events/logs\n\nWithout startedOn, we can't establish a timeline of the build pipeline."}
{"task_id": "task_status_check", "tier": 1, "type": "rule_only", "distilled_think": "This validates that all tasks succeeded. The field is `input.predicate.buildConfig.tasks[*].status`.\n\nPattern:\n```\nsome task in input.predicate.buildConfig.tasks\ntask.status != \"Succeeded\"\n```\n\nThis is a critical check! It fires if any task has status != \"Succeeded\". Possible status values in Tekton:\n- \"Succeeded\" - task completed successfully\n- \"Failed\" - task failed\n- \"Skipped\" - task was skipped (conditional execution)\n- \"Cancelled\" - task was cancelled\n\nFor a valid production attestation, EVERY task must succeed. A failed task means the build is compromised or incomplete. A skipped task might be okay (conditional logic) but this rule treats it as a denial.\n\nThis is probably the most important task-level check - it ensures the build actually succeeded end-to-end."}
{"task_id": "task_step_environment_container_check", "tier": 1, "type": "rule_only", "distilled_think": "This validates container names in task steps using double nesting. The path is `input.predicate.buildConfig.tasks[*].steps[*].environment.container`.\n\nTriple iteration:\n```\nsome task in input.predicate.buildConfig.tasks\nsome step in task.steps\nstep.environment.container != \"init\"\n```\n\nNow we're at tasks → steps → environment.container. Each task can have multiple steps (individual commands/actions), and each step records which container it ran in.\n\nThis requires every step's container to be named \"init\", which is very specific. Container names are typically generated by Kubernetes/Tekton, not user-controlled, so enforcing a specific name suggests this is validating a very controlled environment.\n\nThe triple-nested iteration (tasks, then steps within each task, then field check) is a more complex Rego pattern but follows the same principle."}
{"task_id": "task_step_environment_image_check", "tier": 1, "type": "rule_only", "distilled_think": "This validates the exact container image used by steps. The path is `input.predicate.buildConfig.tasks[*].steps[*].environment.image`.\n\nTriple iteration with a very long expected value:\n```\nsome task in input.predicate.buildConfig.tasks\nsome step in task.steps\nstep.environment.image != \"oci://registry.access.redhat.com/ubi9/skopeo@sha256:75c6ac42431e29465eba3ff3367a18416722cdc18cb7c5745b448f199082fdef\"\n```\n\nThis is enforcing a specific image with a digest pin! The image is Red Hat's UBI9 skopeo image, pinned to a specific SHA256 digest.\n\nDigest pinning is excellent security:\n- Tags can be overwritten (\"latest\" can point to different images)\n- Digests are immutable (sha256:75c6ac... is cryptographically unique)\n- This ensures every step uses the EXACT same image, byte-for-byte\n\nThis prevents supply chain attacks where malicious images are pushed with the same tag. The check fires if any step uses a different image."}
{"task_id": "task_steps_check", "tier": 1, "type": "rule_only", "distilled_think": "This validates that each task has steps. The field is `input.predicate.buildConfig.tasks[*].steps`.\n\nPattern:\n```\nsome task in input.predicate.buildConfig.tasks\nnot task.steps\n```\n\nThis fires if any task is missing its steps array. Steps are the actual commands/actions that execute - without steps, a task does nothing.\n\nA task with no steps might indicate:\n1. Malformed attestation data\n2. Incomplete task execution\n3. Task definition error\n\nNote: this checks for MISSING but not EMPTY. A task with `steps: []` would pass this check but probably shouldn't - a zero-step task is meaningless. To catch both: `not task.steps or count(task.steps) == 0`."}
{"task_id": "type_check", "tier": 1, "type": "rule_only", "distilled_think": "This validates the in-toto statement type at the very top level. The field is `input._type` (note the underscore - this is a top-level field, not under predicate!).\n\nThe expected value is \"https://in-toto.io/Statement/v0.1\" - the in-toto statement wrapper format. SLSA provenance is wrapped in an in-toto statement, so this is the outermost schema declaration.\n\nPattern:\n```\ninput._type != \"https://in-toto.io/Statement/v0.1\"\n```\n\nThis is a foundational check - if _type doesn't match, the entire document structure is wrong. Everything else (predicateType, predicate.buildType, etc.) assumes this is an in-toto statement.\n\nThis is similar to predicateType check, but one level higher in the schema hierarchy:\n- _type establishes we have an in-toto statement\n- predicateType establishes what kind of statement (provenance)\n- predicate.buildType establishes what kind of build (Tekton)\n\nAll three are trust anchors for the document structure."}
