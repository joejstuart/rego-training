{"task_id": "predicate_build_type_check", "tier": 1, "type": "rule_only", "distilled_think": "The user wants to enforce that the buildType field matches a specific Tekton pipeline identifier. Let me break down the requirements:\n\n1. The field path is `input.predicate.buildType` - this is a top-level predicate field, not nested in an array\n2. The expected value is the exact string \"tekton.dev/v1beta1/PipelineRun\"\n3. I need to reject (deny) when it does NOT match this value\n\nSince this is a simple scalar comparison, I don't need iteration - just a direct inequality check. In Rego, I'll use `!=` to catch when the value differs from the expected one.\n\nFor the deny message, I should use sprintf to show both the actual value and what was expected - this makes debugging easier for users. The array argument to sprintf needs to include `input.predicate.buildType` so the actual value is displayed.\n\nPackage name follows the field being checked: `predicate_build_type_check`."}
{"task_id": "predicate_builder_id_check", "tier": 1, "type": "rule_only", "distilled_think": "This rule needs to verify the builder identity URI. The field is at `input.predicate.builder.id` - navigating through predicate, then the builder object, then the id field.\n\nThe expected value is \"https://tekton.dev/chains/v2\" which is the Tekton Chains v2 builder identifier. This is a trust anchor - we only accept attestations from this specific builder.\n\nSince `builder.id` is a simple string field (not an array element), I use a direct comparison without iteration. The deny condition fires when `input.predicate.builder.id != \"https://tekton.dev/chains/v2\"`.\n\nFor the error message, I'll include both the actual and expected values using sprintf. This helps operators quickly identify mismatches - maybe they're using an old builder version, or the attestation came from an untrusted source."}
{"task_id": "predicate_invocation_parameters_git_url_check", "tier": 1, "type": "rule_only", "distilled_think": "The user wants to validate that the source repository URL matches the expected golden container repo. The field path is `input.predicate.invocation.parameters.git-url` - note the hyphenated key \"git-url\" which requires bracket notation in Rego.\n\nThis is checking the source of truth for where the code came from. The expected URL is the enterprise-contract golden-container GitHub repo.\n\nSince `parameters` is an object and \"git-url\" is a key with a hyphen, I must use bracket syntax: `input.predicate.invocation.parameters[\"git-url\"]`. This is not an array iteration - just accessing a map key that contains a special character.\n\nThe deny condition is straightforward: fire when the URL doesn't match the expected value. The sprintf message should show what was found vs. what was expected so operators can debug repository mismatches."}
{"task_id": "predicate_invocation_parameters_hermetic_check", "tier": 1, "type": "rule_only", "distilled_think": "This rule enforces hermetic builds by checking the hermetic flag. The field is at `input.predicate.invocation.parameters.hermetic`.\n\nAn important detail: the expected value is the STRING \"true\", not the boolean true. Looking at the implementation, it compares against the string \"true\" using `!=`. This is correct for SLSA provenance where parameters are often string-typed.\n\nThe check is simple: deny when hermetic is not \"true\". This could catch:\n- hermetic being \"false\" (string)\n- hermetic being false (boolean) - would also fail the string comparison\n- hermetic being missing (undefined) - would also trigger the !=\n\nFor the message, I'll use sprintf to show the actual value. This helps distinguish between \"false\", missing, or unexpected values."}
{"task_id": "predicate_invocation_parameters_output_image_check", "tier": 1, "type": "rule_only", "distilled_think": "This rule checks for the presence of the output-image parameter. Unlike the previous value-matching rules, this one only cares about existence, not a specific value.\n\nThe field path is `input.predicate.invocation.parameters[\"output-image\"]` - again using bracket notation for the hyphenated key.\n\nThe Rego pattern here is `not input.predicate.invocation.parameters[\"output-image\"]`. This is idiomatic for checking field presence in Rego:\n- If the key doesn't exist, accessing it returns undefined, which is falsy, so `not undefined` is true → deny fires ✓\n- If the value is an empty string \"\", that's also falsy, so `not \"\"` is true → deny fires ✓\n- If the value exists and is non-empty, `not <truthy>` is false → no deny ✓\n\nThis single check handles both missing keys and empty values. The message is simple: \"output-image is missing\" since there's no actual value to report."}
{"task_id": "predicate_invocation_parameters_rebuild_check", "tier": 1, "type": "rule_only", "distilled_think": "This enforces that rebuilds are disabled by checking the rebuild parameter. The field is `input.predicate.invocation.parameters.rebuild`.\n\nThe expected value is the string \"false\" (not boolean false). The deny condition fires when rebuild is anything OTHER than the string \"false\". This means:\n- rebuild = \"true\" → deny ✓\n- rebuild = true (boolean) → deny ✓  (string comparison fails)\n- rebuild missing → deny ✓\n- rebuild = \"false\" → no deny ✓\n\nProduction builds should never be rebuilds - they should be fresh, deterministic builds from source. The sprintf message shows the actual value to help debug why the check failed."}
{"task_id": "predicate_invocation_parameters_revision_check", "tier": 1, "type": "rule_only", "distilled_think": "This validates the presence of the git revision (commit SHA). The field is at `input.predicate.invocation.parameters.revision`.\n\nThe instruction mentions \"does not match the expected sha256 digest format\" but the actual implementation only checks for presence using `not input.predicate.invocation.parameters.revision`. This is a reasonable first-level check - ensuring the field exists before validating format.\n\nA more complete implementation would also validate the SHA format (40-char hex for SHA-1 or 64-char hex for SHA-256), but the current rule focuses on the presence check. The field being missing is a critical failure - without a revision, we can't tie the build to a specific commit.\n\nThe message is simple: \"revision is missing\" - clear and actionable."}
{"task_id": "predicate_invocation_parameters_skip_checks_check", "tier": 1, "type": "rule_only", "distilled_think": "This rule prevents bypassing CI checks by enforcing that skip-checks is \"false\". The field path is `input.predicate.invocation.parameters[\"skip-checks\"]` with bracket notation.\n\nProduction attestations must have skip-checks set to \"false\" (string). This is a critical security control - if checks were skipped, we don't know if the build passed linting, testing, security scans, etc.\n\nThe deny fires when skip-checks is anything other than the string \"false\":\n- skip-checks = \"true\" → deny ✓\n- skip-checks missing → deny ✓\n- skip-checks = \"false\" → no deny ✓\n\nThe sprintf message shows the actual value, which helps distinguish between \"true\" (explicitly skipped) and missing (configuration error)."}
{"task_id": "predicate_materials_check", "tier": 1, "type": "rule_only", "distilled_think": "This validates that the materials array exists and is non-empty. Materials represent the build dependencies (base images, libraries, etc.), so they're critical for supply chain verification.\n\nThe field is `input.predicate.materials` and it's expected to be an array. The instruction says \"missing or empty\" but the implementation only checks `not input.predicate.materials`.\n\nIn Rego:\n- If materials doesn't exist → undefined → falsy → `not` it is true → deny ✓\n- If materials is empty array [] → arrays are truthy in Rego → `not []` is false → no deny ✗\n\nSo this implementation catches MISSING but not EMPTY. To also catch empty, you'd need `not input.predicate.materials` OR `count(input.predicate.materials) == 0`. But the current implementation is reasonable if we assume the materials array will always exist in well-formed attestations, just might be absent in malformed ones.\n\nThe message \"materials is missing\" accurately describes what this rule detects."}
{"task_id": "predicate_materials_digest_sha256_check", "tier": 1, "type": "rule_only", "distilled_think": "Now we're iterating! This rule checks that every material has a SHA256 digest. Unlike previous scalar checks, materials is an array, so I need to check each element.\n\nThe Rego pattern is:\n```\nsome material in input.predicate.materials\nnot material.digest.sha256\n```\n\nThis means: \"there exists at least one material where digest.sha256 is missing\". Let me trace through the logic:\n- `some material in input.predicate.materials` binds `material` to each array element\n- `not material.digest.sha256` checks if sha256 is absent\n- If ANY material is missing sha256, the rule body succeeds and deny fires\n\nThe message is generic: \"sha256 is missing\" - it doesn't say WHICH material failed. For better debugging, you could include `material.uri` in the message using sprintf, but the current implementation is simpler.\n\nThe instruction mentions \"does not match the expected sha256 digest format\" but the implementation only checks presence, not format validation (64 hex chars). That's a reasonable separation of concerns - presence check first, format validation could be a separate rule."}
{"task_id": "predicate_materials_uri_check", "tier": 1, "type": "rule_only", "distilled_think": "This validates that a specific base image URI appears in the materials. The expected URI is \"oci://registry.access.redhat.com/ubi9/skopeo\" - the UBI9 skopeo image from Red Hat's registry.\n\nWait, there's something interesting about the implementation:\n```\nsome material in input.predicate.materials\nmaterial.uri != \"oci://registry.access.redhat.com/ubi9/skopeo\"\n```\n\nThis fires if ANY material has a URI that doesn't match the expected value. That means if materials contains multiple entries (common in real builds), this rule would deny unless ALL materials have this exact URI, which seems wrong.\n\nI think the intent is: \"At least one material MUST have this URI\" (existence check), not \"All materials must have this URI\" (universal check). The current implementation is actually doing \"There exists a material with a DIFFERENT URI\" which would be overly restrictive.\n\nA better implementation would be:\n```\nnot contains_expected_material\n```\nwith a helper checking if any material.uri matches.\n\nBut given the code as written, I'll document what it DOES do: denies if any material URI differs from the expected value. The sprintf shows which URI was found."}
{"task_id": "predicate_metadata_build_finished_on_check", "tier": 1, "type": "rule_only", "distilled_think": "This ensures the build finish timestamp is present. The field is `input.predicate.metadata.buildFinishedOn` and contains an ISO-8601 timestamp string.\n\nThe check is `not input.predicate.metadata.buildFinishedOn`, which fires when the field is missing or falsy. The instruction mentions validating \"is not a valid ISO-8601 timestamp\" but the implementation only checks presence.\n\nFor timestamp validation, you'd typically use `time.parse_rfc3339_ns(input.predicate.metadata.buildFinishedOn)` and catch parse errors. But presence checking is a good first line of defense - if the field is completely absent, there's no point trying to parse it.\n\nThe message is straightforward: \"buildFinishedOn is missing\"."}
{"task_id": "predicate_metadata_build_started_on_check", "tier": 1, "type": "rule_only", "distilled_think": "Symmetric to buildFinishedOn - this validates the build start timestamp exists. The field is `input.predicate.metadata.buildStartedOn`.\n\nSame pattern: `not input.predicate.metadata.buildStartedOn` checks presence but not format. These timestamp fields are critical for:\n1. Audit trails - when did the build happen?\n2. Freshness checks - is this attestation recent or stale?\n3. Duration validation - did the build take a reasonable amount of time? (requires both start and finish)\n\nA composite rule could use both timestamps to verify `buildStartedOn < buildFinishedOn` (chronological order), but that would be a tier 3 rule combining multiple fields. This tier 1 rule just ensures the start timestamp exists."}
{"task_id": "predicate_metadata_reproducible_check", "tier": 1, "type": "rule_only", "distilled_think": "This validates the reproducible flag. Interestingly, the expected value is the BOOLEAN false (not string \"false\").\n\nThe check is `input.predicate.metadata.reproducible != false`. This means:\n- reproducible = false (boolean) → no deny ✓\n- reproducible = true (boolean) → deny ✓\n- reproducible missing (undefined) → undefined != false is true → deny ✓\n- reproducible = \"false\" (string) → string != boolean fails comparison → deny ✓\n\nSo this specifically requires the boolean false value. The message \"reproducible must be false\" is clear - Tekton builds are typically not bit-for-bit reproducible due to timestamps, so the attestation should acknowledge this with false.\n\nThis is a good example of type-aware validation in Rego."}
{"task_id": "predicate_type_check", "tier": 1, "type": "rule_only", "distilled_think": "This validates the SLSA predicate type URI at the top level. The field is `input.predicateType` (not nested under predicate!).\n\nThe expected value is \"https://slsa.dev/provenance/v0.2\" - the SLSA provenance v0.2 schema. This is a critical check because it establishes what schema the rest of the attestation should follow. If this doesn't match, the entire attestation structure might be wrong.\n\nThe check is straightforward: `input.predicateType != \"https://slsa.dev/provenance/v0.2\"`. The sprintf message shows both actual and expected values, which helps identify schema version mismatches (maybe someone sent a v1.0 attestation)."}
{"task_id": "subject_digest_sha256_check", "tier": 1, "type": "rule_only", "distilled_think": "This iterates over subjects to validate each has a SHA256 digest. Subjects are the artifacts being attested to (usually container images).\n\nThe pattern:\n```\nsome subject in input.subject\nnot subject.digest.sha256\n```\n\nThis means \"at least one subject is missing digest.sha256\". The digest is critical - it's the cryptographic identity of the artifact. Without it, we can't verify the artifact's integrity.\n\nThe message \"sha256 is missing\" is generic. For better debugging, you could include which subject (by index or by name) failed, but that requires more complex sprintf formatting.\n\nThe instruction mentions validating the digest format (64 hex chars), but the implementation only checks presence. Format validation would require regex or length checks, which could be a separate rule."}
{"task_id": "subject_name_check", "tier": 1, "type": "rule_only", "distilled_think": "This validates that each subject has a name field. The name typically contains the container image reference (e.g., \"quay.io/org/repo/image\").\n\nPattern:\n```\nsome subject in input.subject\nnot subject.name\n```\n\nThis fires if ANY subject is missing its name. Since subjects array often contains just one element (the built image), this is effectively checking that the primary artifact is identifiable.\n\nWithout subject.name, we don't know what artifact the attestation is about - it becomes an orphaned attestation with no clear target. The message is simple: \"name is missing\"."}
{"task_id": "task_finished_on_check", "tier": 1, "type": "rule_only", "distilled_think": "Now we're checking tasks - the individual steps in the Tekton pipeline. This rule validates that each task has a completion timestamp.\n\nThe path is `input.predicate.buildConfig.tasks[*].finishedOn` - tasks is an array under buildConfig.\n\nPattern:\n```\nsome task in input.predicate.buildConfig.tasks\nnot task.finishedOn\n```\n\nThis denies if ANY task is missing finishedOn. In a multi-task pipeline, every task should complete and record its finish time. A missing finishedOn could indicate:\n- The task never finished (build failure)\n- Incomplete attestation data\n- Malformed attestation\n\nThe message \"finishedOn is missing\" is generic across all tasks. For debugging complex pipelines with many tasks, including `task.name` in the message would help identify which task failed."}
{"task_id": "task_invocation_parameters_commit_sha_check", "tier": 1, "type": "rule_only", "distilled_think": "This validates that each task has the COMMIT_SHA parameter recorded. Note this is task-level commit SHA, different from the pipeline-level `invocation.parameters.revision`.\n\nThe path is deeply nested: `input.predicate.buildConfig.tasks[*].invocation.parameters.COMMIT_SHA`.\n\nPattern:\n```\nsome task in input.predicate.buildConfig.tasks\nnot task.invocation.parameters.COMMIT_SHA\n```\n\nIn Tekton, individual tasks can record parameters like COMMIT_SHA to track which commit they operated on. This is especially important for tasks that fetch or build from source.\n\nThe message \"COMMIT_SHA is missing\" fires if any task lacks this parameter. The instruction mentions validating SHA format, but the implementation only checks presence."}
{"task_id": "task_invocation_parameters_dockerfile_check", "tier": 1, "type": "rule_only", "distilled_think": "This validates that build tasks are using the expected Dockerfile name: \"Containerfile\" (the OCI-standard name, not \"Dockerfile\").\n\nThe path is `input.predicate.buildConfig.tasks[*].invocation.parameters.DOCKERFILE`.\n\nPattern:\n```\nsome task in input.predicate.buildConfig.tasks\ntask.invocation.parameters.DOCKERFILE != \"Containerfile\"\n```\n\nThis denies if ANY task has a DOCKERFILE parameter that isn't \"Containerfile\". This enforces a naming convention - using \"Containerfile\" instead of \"Dockerfile\" is the OCI standard and signals that the build uses OCI-compliant tooling.\n\nNote: This would also fire if a task is missing the DOCKERFILE parameter entirely (undefined != \"Containerfile\" is true). If you only want to check tasks that HAVE the parameter, you'd need to add a guard: `task.invocation.parameters.DOCKERFILE; task.invocation.parameters.DOCKERFILE != \"Containerfile\"`.\n\nThe sprintf message shows what was found, which helps identify non-conforming tasks."}
