{"task_id": "policy__lib__json__schema", "user_instruction": "Write positive and negative Rego tests for the helper functions in package `lib.json`.", "new_think_trace": "The user requests tests for a deny rule in package `policy__lib__json__schema` that performs complex validation logic. This may involve cross-field references, helper functions, or multi-step validation.\n\n**Test complexity**: Tier 3 rules often use helper functions to extract or validate data, requiring more sophisticated test data.\n\n**Positive test strategy**:\n- **Assertion**: `count(policy__lib__json__schema.deny) == 0`\n- **Test data requirements**: Must include all fields accessed by the rule AND its helpers\n- **Structure**: May need nested objects, arrays within arrays, or cross-referencing structures\n- **Example**: If the rule validates \"scan tasks have TEST_OUTPUT\", the test data needs:\n  - A tasks array\n  - Tasks with names containing \"scan\"\n  - Corresponding results arrays with TEST_OUTPUT entries\n\n**Negative test strategy**:\n- **Assertion**: `count(policy__lib__json__schema.deny) > 0`\n- **Violation scenarios**: Identify the specific failure mode (missing field, wrong value, failed cross-reference)\n- **Test focus**: Each negative test should target ONE specific violation to make failures easy to diagnose\n\n**Helper function awareness**: If the rule uses helpers like `_has_result()` or `_is_scan_task()`, the test data must be structured so these helpers work correctly.\n\n**SLSA format considerations**: Complex rules often span multiple SLSA v0.2 or v1.0 structures, so test data might need to mock significant portions of the attestation.", "assistant_code": "package phase6_policy_lib_smoke_test\n\nimport rego.v1\n\nimport data.lib.json as target\n\ntest_module_loads if {\n\t_ := target\n}"}
{"task_id": "policy__lib__k8s__k8s", "user_instruction": "Write tests that verify helper behavior (expected and failure cases) for package `lib.k8s`.", "new_think_trace": "The user requests tests for a deny rule in package `policy__lib__k8s__k8s` that performs complex validation logic. This may involve cross-field references, helper functions, or multi-step validation.\n\n**Test complexity**: Tier 3 rules often use helper functions to extract or validate data, requiring more sophisticated test data.\n\n**Positive test strategy**:\n- **Assertion**: `count(policy__lib__k8s__k8s.deny) == 0`\n- **Test data requirements**: Must include all fields accessed by the rule AND its helpers\n- **Structure**: May need nested objects, arrays within arrays, or cross-referencing structures\n- **Example**: If the rule validates \"scan tasks have TEST_OUTPUT\", the test data needs:\n  - A tasks array\n  - Tasks with names containing \"scan\"\n  - Corresponding results arrays with TEST_OUTPUT entries\n\n**Negative test strategy**:\n- **Assertion**: `count(policy__lib__k8s__k8s.deny) > 0`\n- **Violation scenarios**: Identify the specific failure mode (missing field, wrong value, failed cross-reference)\n- **Test focus**: Each negative test should target ONE specific violation to make failures easy to diagnose\n\n**Helper function awareness**: If the rule uses helpers like `_has_result()` or `_is_scan_task()`, the test data must be structured so these helpers work correctly.\n\n**SLSA format considerations**: Complex rules often span multiple SLSA v0.2 or v1.0 structures, so test data might need to mock significant portions of the attestation.", "assistant_code": "package phase6_policy_lib_smoke_test\n\nimport rego.v1\n\nimport data.lib.k8s as target\n\ntest_module_loads if {\n\t_ := target\n}"}
{"task_id": "policy__lib__konflux__konflux", "user_instruction": "Write positive and negative Rego tests for the helper functions in package `lib.konflux`.", "new_think_trace": "The user requests tests for a deny rule in package `policy__lib__konflux__konflux` that performs complex validation logic. This may involve cross-field references, helper functions, or multi-step validation.\n\n**Test complexity**: Tier 3 rules often use helper functions to extract or validate data, requiring more sophisticated test data.\n\n**Positive test strategy**:\n- **Assertion**: `count(policy__lib__konflux__konflux.deny) == 0`\n- **Test data requirements**: Must include all fields accessed by the rule AND its helpers\n- **Structure**: May need nested objects, arrays within arrays, or cross-referencing structures\n- **Example**: If the rule validates \"scan tasks have TEST_OUTPUT\", the test data needs:\n  - A tasks array\n  - Tasks with names containing \"scan\"\n  - Corresponding results arrays with TEST_OUTPUT entries\n\n**Negative test strategy**:\n- **Assertion**: `count(policy__lib__konflux__konflux.deny) > 0`\n- **Violation scenarios**: Identify the specific failure mode (missing field, wrong value, failed cross-reference)\n- **Test focus**: Each negative test should target ONE specific violation to make failures easy to diagnose\n\n**Helper function awareness**: If the rule uses helpers like `_has_result()` or `_is_scan_task()`, the test data must be structured so these helpers work correctly.\n\n**SLSA format considerations**: Complex rules often span multiple SLSA v0.2 or v1.0 structures, so test data might need to mock significant portions of the attestation.", "assistant_code": "package phase6_policy_lib_smoke_test\n\nimport rego.v1\n\nimport data.lib.konflux as target\n\ntest_module_loads if {\n\t_ := target\n}"}
{"task_id": "policy__lib__metadata_helper", "user_instruction": "Write positive and negative Rego tests for the helper functions in package `lib`.", "new_think_trace": "The user requests tests for a deny rule in package `policy__lib__metadata_helper` that performs complex validation logic. This may involve cross-field references, helper functions, or multi-step validation.\n\n**Test complexity**: Tier 3 rules often use helper functions to extract or validate data, requiring more sophisticated test data.\n\n**Positive test strategy**:\n- **Assertion**: `count(policy__lib__metadata_helper.deny) == 0`\n- **Test data requirements**: Must include all fields accessed by the rule AND its helpers\n- **Structure**: May need nested objects, arrays within arrays, or cross-referencing structures\n- **Example**: If the rule validates \"scan tasks have TEST_OUTPUT\", the test data needs:\n  - A tasks array\n  - Tasks with names containing \"scan\"\n  - Corresponding results arrays with TEST_OUTPUT entries\n\n**Negative test strategy**:\n- **Assertion**: `count(policy__lib__metadata_helper.deny) > 0`\n- **Violation scenarios**: Identify the specific failure mode (missing field, wrong value, failed cross-reference)\n- **Test focus**: Each negative test should target ONE specific violation to make failures easy to diagnose\n\n**Helper function awareness**: If the rule uses helpers like `_has_result()` or `_is_scan_task()`, the test data must be structured so these helpers work correctly.\n\n**SLSA format considerations**: Complex rules often span multiple SLSA v0.2 or v1.0 structures, so test data might need to mock significant portions of the attestation.", "assistant_code": "package phase6_policy_lib_smoke_test\n\nimport rego.v1\n\nimport data.lib as target\n\ntest_module_loads if {\n\t_ := target\n}"}
{"task_id": "policy__lib__rule_data", "user_instruction": "Write tests that verify helper behavior (expected and failure cases) for package `lib`.", "new_think_trace": "The user requests tests for a deny rule in package `policy__lib__rule_data` that performs complex validation logic. This may involve cross-field references, helper functions, or multi-step validation.\n\n**Test complexity**: Tier 3 rules often use helper functions to extract or validate data, requiring more sophisticated test data.\n\n**Positive test strategy**:\n- **Assertion**: `count(policy__lib__rule_data.deny) == 0`\n- **Test data requirements**: Must include all fields accessed by the rule AND its helpers\n- **Structure**: May need nested objects, arrays within arrays, or cross-referencing structures\n- **Example**: If the rule validates \"scan tasks have TEST_OUTPUT\", the test data needs:\n  - A tasks array\n  - Tasks with names containing \"scan\"\n  - Corresponding results arrays with TEST_OUTPUT entries\n\n**Negative test strategy**:\n- **Assertion**: `count(policy__lib__rule_data.deny) > 0`\n- **Violation scenarios**: Identify the specific failure mode (missing field, wrong value, failed cross-reference)\n- **Test focus**: Each negative test should target ONE specific violation to make failures easy to diagnose\n\n**Helper function awareness**: If the rule uses helpers like `_has_result()` or `_is_scan_task()`, the test data must be structured so these helpers work correctly.\n\n**SLSA format considerations**: Complex rules often span multiple SLSA v0.2 or v1.0 structures, so test data might need to mock significant portions of the attestation.", "assistant_code": "package phase6_policy_lib_smoke_test\n\nimport rego.v1\n\nimport data.lib as target\n\ntest_module_loads if {\n\t_ := target\n}"}
{"task_id": "policy__lib__sbom__rpm", "user_instruction": "Write tests that verify helper behavior (expected and failure cases) for package `lib.sbom`.", "new_think_trace": "The user requests tests for a deny rule in package `policy__lib__sbom__rpm` that performs complex validation logic. This may involve cross-field references, helper functions, or multi-step validation.\n\n**Test complexity**: Tier 3 rules often use helper functions to extract or validate data, requiring more sophisticated test data.\n\n**Positive test strategy**:\n- **Assertion**: `count(policy__lib__sbom__rpm.deny) == 0`\n- **Test data requirements**: Must include all fields accessed by the rule AND its helpers\n- **Structure**: May need nested objects, arrays within arrays, or cross-referencing structures\n- **Example**: If the rule validates \"scan tasks have TEST_OUTPUT\", the test data needs:\n  - A tasks array\n  - Tasks with names containing \"scan\"\n  - Corresponding results arrays with TEST_OUTPUT entries\n\n**Negative test strategy**:\n- **Assertion**: `count(policy__lib__sbom__rpm.deny) > 0`\n- **Violation scenarios**: Identify the specific failure mode (missing field, wrong value, failed cross-reference)\n- **Test focus**: Each negative test should target ONE specific violation to make failures easy to diagnose\n\n**Helper function awareness**: If the rule uses helpers like `_has_result()` or `_is_scan_task()`, the test data must be structured so these helpers work correctly.\n\n**SLSA format considerations**: Complex rules often span multiple SLSA v0.2 or v1.0 structures, so test data might need to mock significant portions of the attestation.", "assistant_code": "package phase6_policy_lib_smoke_test\n\nimport rego.v1\n\nimport data.lib.sbom as target\n\ntest_module_loads if {\n\t_ := target\n}"}
{"task_id": "policy__lib__sbom__sbom", "user_instruction": "Write tests that verify helper behavior (expected and failure cases) for package `lib.sbom`.", "new_think_trace": "The user requests tests for a deny rule in package `policy__lib__sbom__sbom` that performs complex validation logic. This may involve cross-field references, helper functions, or multi-step validation.\n\n**Test complexity**: Tier 3 rules often use helper functions to extract or validate data, requiring more sophisticated test data.\n\n**Positive test strategy**:\n- **Assertion**: `count(policy__lib__sbom__sbom.deny) == 0`\n- **Test data requirements**: Must include all fields accessed by the rule AND its helpers\n- **Structure**: May need nested objects, arrays within arrays, or cross-referencing structures\n- **Example**: If the rule validates \"scan tasks have TEST_OUTPUT\", the test data needs:\n  - A tasks array\n  - Tasks with names containing \"scan\"\n  - Corresponding results arrays with TEST_OUTPUT entries\n\n**Negative test strategy**:\n- **Assertion**: `count(policy__lib__sbom__sbom.deny) > 0`\n- **Violation scenarios**: Identify the specific failure mode (missing field, wrong value, failed cross-reference)\n- **Test focus**: Each negative test should target ONE specific violation to make failures easy to diagnose\n\n**Helper function awareness**: If the rule uses helpers like `_has_result()` or `_is_scan_task()`, the test data must be structured so these helpers work correctly.\n\n**SLSA format considerations**: Complex rules often span multiple SLSA v0.2 or v1.0 structures, so test data might need to mock significant portions of the attestation.", "assistant_code": "package phase6_policy_lib_smoke_test\n\nimport rego.v1\n\nimport data.lib.sbom as target\n\ntest_module_loads if {\n\t_ := target\n}"}
{"task_id": "policy__lib__set_helpers", "user_instruction": "Write positive and negative Rego tests for the helper functions in package `lib`.", "new_think_trace": "The user requests tests for a deny rule in package `policy__lib__set_helpers` that performs complex validation logic. This may involve cross-field references, helper functions, or multi-step validation.\n\n**Test complexity**: Tier 3 rules often use helper functions to extract or validate data, requiring more sophisticated test data.\n\n**Positive test strategy**:\n- **Assertion**: `count(policy__lib__set_helpers.deny) == 0`\n- **Test data requirements**: Must include all fields accessed by the rule AND its helpers\n- **Structure**: May need nested objects, arrays within arrays, or cross-referencing structures\n- **Example**: If the rule validates \"scan tasks have TEST_OUTPUT\", the test data needs:\n  - A tasks array\n  - Tasks with names containing \"scan\"\n  - Corresponding results arrays with TEST_OUTPUT entries\n\n**Negative test strategy**:\n- **Assertion**: `count(policy__lib__set_helpers.deny) > 0`\n- **Violation scenarios**: Identify the specific failure mode (missing field, wrong value, failed cross-reference)\n- **Test focus**: Each negative test should target ONE specific violation to make failures easy to diagnose\n\n**Helper function awareness**: If the rule uses helpers like `_has_result()` or `_is_scan_task()`, the test data must be structured so these helpers work correctly.\n\n**SLSA format considerations**: Complex rules often span multiple SLSA v0.2 or v1.0 structures, so test data might need to mock significant portions of the attestation.", "assistant_code": "package phase6_policy_lib_smoke_test\n\nimport rego.v1\n\nimport data.lib as target\n\ntest_module_loads if {\n\t_ := target\n}"}
{"task_id": "policy__lib__sigstore", "user_instruction": "Write positive and negative Rego tests for the helper functions in package `lib`.", "new_think_trace": "The user requests tests for a deny rule in package `policy__lib__sigstore` that performs complex validation logic. This may involve cross-field references, helper functions, or multi-step validation.\n\n**Test complexity**: Tier 3 rules often use helper functions to extract or validate data, requiring more sophisticated test data.\n\n**Positive test strategy**:\n- **Assertion**: `count(policy__lib__sigstore.deny) == 0`\n- **Test data requirements**: Must include all fields accessed by the rule AND its helpers\n- **Structure**: May need nested objects, arrays within arrays, or cross-referencing structures\n- **Example**: If the rule validates \"scan tasks have TEST_OUTPUT\", the test data needs:\n  - A tasks array\n  - Tasks with names containing \"scan\"\n  - Corresponding results arrays with TEST_OUTPUT entries\n\n**Negative test strategy**:\n- **Assertion**: `count(policy__lib__sigstore.deny) > 0`\n- **Violation scenarios**: Identify the specific failure mode (missing field, wrong value, failed cross-reference)\n- **Test focus**: Each negative test should target ONE specific violation to make failures easy to diagnose\n\n**Helper function awareness**: If the rule uses helpers like `_has_result()` or `_is_scan_task()`, the test data must be structured so these helpers work correctly.\n\n**SLSA format considerations**: Complex rules often span multiple SLSA v0.2 or v1.0 structures, so test data might need to mock significant portions of the attestation.", "assistant_code": "package phase6_policy_lib_smoke_test\n\nimport rego.v1\n\nimport data.lib as target\n\ntest_module_loads if {\n\t_ := target\n}"}
{"task_id": "policy__lib__string_utils", "user_instruction": "Write tests that verify helper behavior (expected and failure cases) for package `lib`.", "new_think_trace": "The user requests tests for a deny rule in package `policy__lib__string_utils` that performs complex validation logic. This may involve cross-field references, helper functions, or multi-step validation.\n\n**Test complexity**: Tier 3 rules often use helper functions to extract or validate data, requiring more sophisticated test data.\n\n**Positive test strategy**:\n- **Assertion**: `count(policy__lib__string_utils.deny) == 0`\n- **Test data requirements**: Must include all fields accessed by the rule AND its helpers\n- **Structure**: May need nested objects, arrays within arrays, or cross-referencing structures\n- **Example**: If the rule validates \"scan tasks have TEST_OUTPUT\", the test data needs:\n  - A tasks array\n  - Tasks with names containing \"scan\"\n  - Corresponding results arrays with TEST_OUTPUT entries\n\n**Negative test strategy**:\n- **Assertion**: `count(policy__lib__string_utils.deny) > 0`\n- **Violation scenarios**: Identify the specific failure mode (missing field, wrong value, failed cross-reference)\n- **Test focus**: Each negative test should target ONE specific violation to make failures easy to diagnose\n\n**Helper function awareness**: If the rule uses helpers like `_has_result()` or `_is_scan_task()`, the test data must be structured so these helpers work correctly.\n\n**SLSA format considerations**: Complex rules often span multiple SLSA v0.2 or v1.0 structures, so test data might need to mock significant portions of the attestation.", "assistant_code": "package phase6_policy_lib_smoke_test\n\nimport rego.v1\n\nimport data.lib as target\n\ntest_module_loads if {\n\t_ := target\n}"}
{"task_id": "policy__lib__tekton__bundles", "user_instruction": "Write positive and negative Rego tests for the helper functions in package `lib.tekton`.", "new_think_trace": "The user requests tests for a deny rule in package `policy__lib__tekton__bundles` that performs complex validation logic. This may involve cross-field references, helper functions, or multi-step validation.\n\n**Test complexity**: Tier 3 rules often use helper functions to extract or validate data, requiring more sophisticated test data.\n\n**Positive test strategy**:\n- **Assertion**: `count(policy__lib__tekton__bundles.deny) == 0`\n- **Test data requirements**: Must include all fields accessed by the rule AND its helpers\n- **Structure**: May need nested objects, arrays within arrays, or cross-referencing structures\n- **Example**: If the rule validates \"scan tasks have TEST_OUTPUT\", the test data needs:\n  - A tasks array\n  - Tasks with names containing \"scan\"\n  - Corresponding results arrays with TEST_OUTPUT entries\n\n**Negative test strategy**:\n- **Assertion**: `count(policy__lib__tekton__bundles.deny) > 0`\n- **Violation scenarios**: Identify the specific failure mode (missing field, wrong value, failed cross-reference)\n- **Test focus**: Each negative test should target ONE specific violation to make failures easy to diagnose\n\n**Helper function awareness**: If the rule uses helpers like `_has_result()` or `_is_scan_task()`, the test data must be structured so these helpers work correctly.\n\n**SLSA format considerations**: Complex rules often span multiple SLSA v0.2 or v1.0 structures, so test data might need to mock significant portions of the attestation.", "assistant_code": "package phase6_policy_lib_smoke_test\n\nimport rego.v1\n\nimport data.lib.tekton as target\n\ntest_module_loads if {\n\t_ := target\n}"}
{"task_id": "policy__lib__tekton__pipeline", "user_instruction": "Write positive and negative Rego tests for the helper functions in package `lib.tekton`.", "new_think_trace": "The user requests tests for a deny rule in package `policy__lib__tekton__pipeline` that performs complex validation logic. This may involve cross-field references, helper functions, or multi-step validation.\n\n**Test complexity**: Tier 3 rules often use helper functions to extract or validate data, requiring more sophisticated test data.\n\n**Positive test strategy**:\n- **Assertion**: `count(policy__lib__tekton__pipeline.deny) == 0`\n- **Test data requirements**: Must include all fields accessed by the rule AND its helpers\n- **Structure**: May need nested objects, arrays within arrays, or cross-referencing structures\n- **Example**: If the rule validates \"scan tasks have TEST_OUTPUT\", the test data needs:\n  - A tasks array\n  - Tasks with names containing \"scan\"\n  - Corresponding results arrays with TEST_OUTPUT entries\n\n**Negative test strategy**:\n- **Assertion**: `count(policy__lib__tekton__pipeline.deny) > 0`\n- **Violation scenarios**: Identify the specific failure mode (missing field, wrong value, failed cross-reference)\n- **Test focus**: Each negative test should target ONE specific violation to make failures easy to diagnose\n\n**Helper function awareness**: If the rule uses helpers like `_has_result()` or `_is_scan_task()`, the test data must be structured so these helpers work correctly.\n\n**SLSA format considerations**: Complex rules often span multiple SLSA v0.2 or v1.0 structures, so test data might need to mock significant portions of the attestation.", "assistant_code": "package phase6_policy_lib_smoke_test\n\nimport rego.v1\n\nimport data.lib.tekton as target\n\ntest_module_loads if {\n\t_ := target\n}"}
{"task_id": "policy__lib__tekton__refs", "user_instruction": "Write positive and negative Rego tests for the helper functions in package `lib.tekton`.", "new_think_trace": "The user requests tests for a deny rule in package `policy__lib__tekton__refs` that performs complex validation logic. This may involve cross-field references, helper functions, or multi-step validation.\n\n**Test complexity**: Tier 3 rules often use helper functions to extract or validate data, requiring more sophisticated test data.\n\n**Positive test strategy**:\n- **Assertion**: `count(policy__lib__tekton__refs.deny) == 0`\n- **Test data requirements**: Must include all fields accessed by the rule AND its helpers\n- **Structure**: May need nested objects, arrays within arrays, or cross-referencing structures\n- **Example**: If the rule validates \"scan tasks have TEST_OUTPUT\", the test data needs:\n  - A tasks array\n  - Tasks with names containing \"scan\"\n  - Corresponding results arrays with TEST_OUTPUT entries\n\n**Negative test strategy**:\n- **Assertion**: `count(policy__lib__tekton__refs.deny) > 0`\n- **Violation scenarios**: Identify the specific failure mode (missing field, wrong value, failed cross-reference)\n- **Test focus**: Each negative test should target ONE specific violation to make failures easy to diagnose\n\n**Helper function awareness**: If the rule uses helpers like `_has_result()` or `_is_scan_task()`, the test data must be structured so these helpers work correctly.\n\n**SLSA format considerations**: Complex rules often span multiple SLSA v0.2 or v1.0 structures, so test data might need to mock significant portions of the attestation.", "assistant_code": "package phase6_policy_lib_smoke_test\n\nimport rego.v1\n\nimport data.lib.tekton as target\n\ntest_module_loads if {\n\t_ := target\n}"}
{"task_id": "policy__lib__tekton__task", "user_instruction": "Write tests that verify helper behavior (expected and failure cases) for package `lib.tekton`.", "new_think_trace": "The user requests tests for a deny rule in package `policy__lib__tekton__task` that performs complex validation logic. This may involve cross-field references, helper functions, or multi-step validation.\n\n**Test complexity**: Tier 3 rules often use helper functions to extract or validate data, requiring more sophisticated test data.\n\n**Positive test strategy**:\n- **Assertion**: `count(policy__lib__tekton__task.deny) == 0`\n- **Test data requirements**: Must include all fields accessed by the rule AND its helpers\n- **Structure**: May need nested objects, arrays within arrays, or cross-referencing structures\n- **Example**: If the rule validates \"scan tasks have TEST_OUTPUT\", the test data needs:\n  - A tasks array\n  - Tasks with names containing \"scan\"\n  - Corresponding results arrays with TEST_OUTPUT entries\n\n**Negative test strategy**:\n- **Assertion**: `count(policy__lib__tekton__task.deny) > 0`\n- **Violation scenarios**: Identify the specific failure mode (missing field, wrong value, failed cross-reference)\n- **Test focus**: Each negative test should target ONE specific violation to make failures easy to diagnose\n\n**Helper function awareness**: If the rule uses helpers like `_has_result()` or `_is_scan_task()`, the test data must be structured so these helpers work correctly.\n\n**SLSA format considerations**: Complex rules often span multiple SLSA v0.2 or v1.0 structures, so test data might need to mock significant portions of the attestation.", "assistant_code": "package phase6_policy_lib_smoke_test\n\nimport rego.v1\n\nimport data.lib.tekton as target\n\ntest_module_loads if {\n\t_ := target\n}"}
{"task_id": "policy__lib__tekton__task_results", "user_instruction": "Write positive and negative Rego tests for the helper functions in package `lib.tekton`.", "new_think_trace": "The user requests tests for a deny rule in package `policy__lib__tekton__task_results` that performs complex validation logic. This may involve cross-field references, helper functions, or multi-step validation.\n\n**Test complexity**: Tier 3 rules often use helper functions to extract or validate data, requiring more sophisticated test data.\n\n**Positive test strategy**:\n- **Assertion**: `count(policy__lib__tekton__task_results.deny) == 0`\n- **Test data requirements**: Must include all fields accessed by the rule AND its helpers\n- **Structure**: May need nested objects, arrays within arrays, or cross-referencing structures\n- **Example**: If the rule validates \"scan tasks have TEST_OUTPUT\", the test data needs:\n  - A tasks array\n  - Tasks with names containing \"scan\"\n  - Corresponding results arrays with TEST_OUTPUT entries\n\n**Negative test strategy**:\n- **Assertion**: `count(policy__lib__tekton__task_results.deny) > 0`\n- **Violation scenarios**: Identify the specific failure mode (missing field, wrong value, failed cross-reference)\n- **Test focus**: Each negative test should target ONE specific violation to make failures easy to diagnose\n\n**Helper function awareness**: If the rule uses helpers like `_has_result()` or `_is_scan_task()`, the test data must be structured so these helpers work correctly.\n\n**SLSA format considerations**: Complex rules often span multiple SLSA v0.2 or v1.0 structures, so test data might need to mock significant portions of the attestation.", "assistant_code": "package phase6_policy_lib_smoke_test\n\nimport rego.v1\n\nimport data.lib.tekton as target\n\ntest_module_loads if {\n\t_ := target\n}"}
{"task_id": "policy__lib__tekton__trusted", "user_instruction": "Write tests that verify helper behavior (expected and failure cases) for package `lib.tekton`.", "new_think_trace": "The user requests tests for a deny rule in package `policy__lib__tekton__trusted` that performs complex validation logic. This may involve cross-field references, helper functions, or multi-step validation.\n\n**Test complexity**: Tier 3 rules often use helper functions to extract or validate data, requiring more sophisticated test data.\n\n**Positive test strategy**:\n- **Assertion**: `count(policy__lib__tekton__trusted.deny) == 0`\n- **Test data requirements**: Must include all fields accessed by the rule AND its helpers\n- **Structure**: May need nested objects, arrays within arrays, or cross-referencing structures\n- **Example**: If the rule validates \"scan tasks have TEST_OUTPUT\", the test data needs:\n  - A tasks array\n  - Tasks with names containing \"scan\"\n  - Corresponding results arrays with TEST_OUTPUT entries\n\n**Negative test strategy**:\n- **Assertion**: `count(policy__lib__tekton__trusted.deny) > 0`\n- **Violation scenarios**: Identify the specific failure mode (missing field, wrong value, failed cross-reference)\n- **Test focus**: Each negative test should target ONE specific violation to make failures easy to diagnose\n\n**Helper function awareness**: If the rule uses helpers like `_has_result()` or `_is_scan_task()`, the test data must be structured so these helpers work correctly.\n\n**SLSA format considerations**: Complex rules often span multiple SLSA v0.2 or v1.0 structures, so test data might need to mock significant portions of the attestation.", "assistant_code": "package phase6_policy_lib_smoke_test\n\nimport rego.v1\n\nimport data.lib.tekton as target\n\ntest_module_loads if {\n\t_ := target\n}"}
{"task_id": "policy__lib__time__time", "user_instruction": "Write tests that verify helper behavior (expected and failure cases) for package `lib.time`.", "new_think_trace": "The user requests tests for a deny rule in package `policy__lib__time__time` that performs complex validation logic. This may involve cross-field references, helper functions, or multi-step validation.\n\n**Test complexity**: Tier 3 rules often use helper functions to extract or validate data, requiring more sophisticated test data.\n\n**Positive test strategy**:\n- **Assertion**: `count(policy__lib__time__time.deny) == 0`\n- **Test data requirements**: Must include all fields accessed by the rule AND its helpers\n- **Structure**: May need nested objects, arrays within arrays, or cross-referencing structures\n- **Example**: If the rule validates \"scan tasks have TEST_OUTPUT\", the test data needs:\n  - A tasks array\n  - Tasks with names containing \"scan\"\n  - Corresponding results arrays with TEST_OUTPUT entries\n\n**Negative test strategy**:\n- **Assertion**: `count(policy__lib__time__time.deny) > 0`\n- **Violation scenarios**: Identify the specific failure mode (missing field, wrong value, failed cross-reference)\n- **Test focus**: Each negative test should target ONE specific violation to make failures easy to diagnose\n\n**Helper function awareness**: If the rule uses helpers like `_has_result()` or `_is_scan_task()`, the test data must be structured so these helpers work correctly.\n\n**SLSA format considerations**: Complex rules often span multiple SLSA v0.2 or v1.0 structures, so test data might need to mock significant portions of the attestation.", "assistant_code": "package phase6_policy_lib_smoke_test\n\nimport rego.v1\n\nimport data.lib.time as target\n\ntest_module_loads if {\n\t_ := target\n}"}
{"task_id": "predicate_build_type_check", "user_instruction": "Write Rego tests for code that rejects the attestation if `.predicate.buildType` is not `\"tekton.dev/v1beta1/PipelineRun\"`. (Build system type identifier)", "new_think_trace": "The user requests Rego unit tests for a deny rule in package `predicate_build_type_check`. This is test code that validates the policy rule's behavior with both valid and invalid inputs.\n\n**Test structure**: Rego tests use the `test_` prefix and must evaluate to `true` to pass. I need to write both positive and negative test cases.\n\n**Positive test** (`test_predicate_build_type_check_valid`):\n- **Purpose**: Verify that valid input produces NO deny violations\n- **Assertion**: `count(predicate_build_type_check.deny) == 0`\n- **Test data**: Create minimal JSON containing only the field(s) the rule accesses\n- **Use `with input as {...}`**: This temporarily replaces the global `input` document during test evaluation\n\n**Negative test** (`test_predicate_build_type_check_invalid`):\n- **Purpose**: Verify that invalid input DOES produce deny violations\n- **Assertion**: `count(predicate_build_type_check.deny) > 0`\n- **Test data**: Use invalid values (e.g., \"INVALID_VALUE\", wrong URLs, wrong booleans) to trigger the denial\n\n**Minimal test data philosophy**: Only include fields the rule actually accesses. This reduces test brittleness and makes it clear which fields the rule depends on.\n\n**Import requirement**: `import data.predicate_build_type_check` provides access to the package under test. The deny set is then accessible as `predicate_build_type_check.deny`.", "assistant_code": "package predicate_build_type_check_test\n\nimport rego.v1\n\nimport data.predicate_build_type_check\n\n# Instruction: Write a Rego deny rule that rejects the attestation if `.predicate.buildType` is not `\"tekton.dev/v1beta1/PipelineRun\"`. (Build system type identifier)\n\n# Positive test: valid input should produce no deny violations.\ntest_predicate_build_type_check_valid if {\n\tcount(predicate_build_type_check.deny) == 0\n\twith input as {\n\t\t\"predicate\": {\n\t\t\t\"buildType\": \"tekton.dev/v1beta1/PipelineRun\",\n\t\t},\n\t}\n}\n\n# Negative test: invalid input should produce at least one deny violation.\ntest_predicate_build_type_check_invalid if {\n\tcount(predicate_build_type_check.deny) > 0\n\twith input as {\n\t\t\"predicate\": {\n\t\t\t\"buildType\": \"INVALID_VALUE\",\n\t\t},\n\t}\n}"}
{"task_id": "predicate_builder_id_check", "user_instruction": "Write Rego tests for code that rejects the attestation if `.predicate.builder.id` is not `\"https://tekton.dev/chains/v2\"`. (Builder identity URI)", "new_think_trace": "The user requests Rego unit tests for a deny rule in package `predicate_builder_id_check`. This is test code that validates the policy rule's behavior with both valid and invalid inputs.\n\n**Test structure**: Rego tests use the `test_` prefix and must evaluate to `true` to pass. I need to write both positive and negative test cases.\n\n**Positive test** (`test_predicate_builder_id_check_valid`):\n- **Purpose**: Verify that valid input produces NO deny violations\n- **Assertion**: `count(predicate_builder_id_check.deny) == 0`\n- **Test data**: Create minimal JSON containing only the field(s) the rule accesses\n- **Use `with input as {...}`**: This temporarily replaces the global `input` document during test evaluation\n\n**Negative test** (`test_predicate_builder_id_check_invalid`):\n- **Purpose**: Verify that invalid input DOES produce deny violations\n- **Assertion**: `count(predicate_builder_id_check.deny) > 0`\n- **Test data**: Use invalid values (e.g., \"INVALID_VALUE\", wrong URLs, wrong booleans) to trigger the denial\n\n**Minimal test data philosophy**: Only include fields the rule actually accesses. This reduces test brittleness and makes it clear which fields the rule depends on.\n\n**Import requirement**: `import data.predicate_builder_id_check` provides access to the package under test. The deny set is then accessible as `predicate_builder_id_check.deny`.", "assistant_code": "package predicate_builder_id_check_test\n\nimport rego.v1\n\nimport data.predicate_builder_id_check\n\n# Instruction: Write a Rego deny rule that rejects the attestation if `.predicate.builder.id` is not `\"https://tekton.dev/chains/v2\"`. (Builder identity URI)\n\n# Positive test: valid input should produce no deny violations.\ntest_predicate_builder_id_check_valid if {\n\tcount(predicate_builder_id_check.deny) == 0\n\twith input as {\n\t\t\"predicate\": {\n\t\t\t\"builder\": {\n\t\t\t\t\"id\": \"https://tekton.dev/chains/v2\",\n\t\t\t},\n\t\t},\n\t}\n}\n\n# Negative test: invalid input should produce at least one deny violation.\ntest_predicate_builder_id_check_invalid if {\n\tcount(predicate_builder_id_check.deny) > 0\n\twith input as {\n\t\t\"predicate\": {\n\t\t\t\"builder\": {\n\t\t\t\t\"id\": \"https://example.com/INVALID\",\n\t\t\t},\n\t\t},\n\t}\n}"}
{"task_id": "predicate_invocation_parameters_git_url_check", "user_instruction": "Write Rego tests for code that rejects the attestation if `.predicate.invocation.parameters.git-url` is not `\"https://github.com/enterprise-contract/golden-container\"`. (Predicate field: invocation.parameters.git-url)", "new_think_trace": "The user requests Rego unit tests for a deny rule in package `predicate_invocation_parameters_git_url_check`. This is test code that validates the policy rule's behavior with both valid and invalid inputs.\n\n**Test structure**: Rego tests use the `test_` prefix and must evaluate to `true` to pass. I need to write both positive and negative test cases.\n\n**Positive test** (`test_predicate_invocation_parameters_git_url_check_valid`):\n- **Purpose**: Verify that valid input produces NO deny violations\n- **Assertion**: `count(predicate_invocation_parameters_git_url_check.deny) == 0`\n- **Test data**: Create minimal JSON containing only the field(s) the rule accesses\n- **Use `with input as {...}`**: This temporarily replaces the global `input` document during test evaluation\n\n**Negative test** (`test_predicate_invocation_parameters_git_url_check_invalid`):\n- **Purpose**: Verify that invalid input DOES produce deny violations\n- **Assertion**: `count(predicate_invocation_parameters_git_url_check.deny) > 0`\n- **Test data**: Use invalid values (e.g., \"INVALID_VALUE\", wrong URLs, wrong booleans) to trigger the denial\n\n**Minimal test data philosophy**: Only include fields the rule actually accesses. This reduces test brittleness and makes it clear which fields the rule depends on.\n\n**Import requirement**: `import data.predicate_invocation_parameters_git_url_check` provides access to the package under test. The deny set is then accessible as `predicate_invocation_parameters_git_url_check.deny`.", "assistant_code": "package predicate_invocation_parameters_git_url_check_test\n\nimport rego.v1\n\nimport data.predicate_invocation_parameters_git_url_check\n\n# Instruction: Write a Rego deny rule that rejects the attestation if `.predicate.invocation.parameters.git-url` is not `\"https://github.com/enterprise-contract/golden-container\"`. (Predicate field: invocation.parameters.git-url)\n\n# Positive test: valid input should produce no deny violations.\ntest_predicate_invocation_parameters_git_url_check_valid if {\n\tcount(predicate_invocation_parameters_git_url_check.deny) == 0\n\twith input as {\n\t\t\"predicate\": {\n\t\t\t\"invocation\": {\n\t\t\t\t\"parameters\": {\n\t\t\t\t\t\"git-url\": \"https://github.com/enterprise-contract/golden-container\",\n\t\t\t\t},\n\t\t\t},\n\t\t},\n\t}\n}\n\n# Negative test: invalid input should produce at least one deny violation.\ntest_predicate_invocation_parameters_git_url_check_invalid if {\n\tcount(predicate_invocation_parameters_git_url_check.deny) > 0\n\twith input as {\n\t\t\"predicate\": {\n\t\t\t\"invocation\": {\n\t\t\t\t\"parameters\": {\n\t\t\t\t\t\"git-url\": \"https://example.com/INVALID\",\n\t\t\t\t},\n\t\t\t},\n\t\t},\n\t}\n}"}
