"""GRPO training package — Stage 2 of the Rego expert training pipeline.

Modules:
    rewards        — 5 reward functions for GRPO (format, opa_parse, opa_test, schema_paths, regal_lint)
    build_dataset  — assembles the GRPO prompt dataset from SFT phases
    train          — GRPOTrainer driver
"""
